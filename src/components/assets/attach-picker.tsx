"use client";
// CONTRACT STUB: the assets agent replaces this file with the real picker. Keep these props.
export type AttachedFile = { attachmentId: string; assetId: string; filename: string; kind: "image" | "video" | "pdf" };

export function AttachPicker(_props: { workspaceId: string; taskId?: string; sectionId?: string; attached: AttachedFile[]; readOnly?: boolean }) {
  return null;
}
